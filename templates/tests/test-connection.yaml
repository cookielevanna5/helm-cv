apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-cv.fullname" . }}-test-connection"
  labels:
    {{- include "helm-cv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-cv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
